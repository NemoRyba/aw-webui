// Fork-specific feature switches.
// Stopwatch code is intentionally retained for upstream compatibility,
// but the UI is hidden in this fork because the feature is not used.
export const FORK_FEATURES = {
  stopwatchEnabled: false,
} as const;
